server:
  port: 80
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://192.168.181.128:3306/hotel_pms?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 格式化全局时间字段
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
logging:
  config: classpath:logback-local.xml

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.hotel.pms.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl